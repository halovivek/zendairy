
{
  "name": "zendiary-mobile",
  "version": "1.0.0",
  "description": "ZenDiary Personal Sanctuary Mobile",
  "main": "index.js",
  "dependencies": {
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "@google/genai": "^1.41.0",
    "recharts": "^3.7.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/haptics": "^6.0.0",
    "@capacitor/keyboard": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "mkdir -p dist && cp index.html index.tsx App.tsx types.ts metadata.json dist/ && cp -rv components services dist/ && echo 'Web assets ready in dist/'",
    "cap-add": "npx cap add android",
    "sync": "npm run build && npx cap sync android",
    "open:android": "npx cap open android"
  }
}
