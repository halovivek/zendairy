{
  "name": "zendiary-mobile",
  "version": "1.0.0",
  "description": "ZenDiary Personal Sanctuary Mobile",
  "main": "index.js",
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/core": "^6.0.0",
    "@google/genai": "^1.41.0",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "recharts": "^3.7.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.1.0"
  },
  "scripts": {
  "build": "mkdir -p dist && cp index.html dist/ && cp -r components dist/ 2>/dev/null || : && cp -r services dist/ 2>/dev/null || : && echo 'Web assets ready in dist/'",
  "test": "echo \"Error: no test specified\" && exit 1"
 }
}
